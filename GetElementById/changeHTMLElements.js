let my_id = document.getElementById("para1")
console.log(my_id)
my_id.innerText = "This is the updated one"  // Html elements can't be used here as it considers it as text.
// my_id.innerHTML = "I <strong> like </strong> coding"